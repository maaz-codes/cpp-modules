#ifndef CONTACT_H
#define CONTACT_H

#include <iostream>

class Contact {
    public :
    int         index;
    std::string number;
    std::string f_name;
    std::string l_name;
    std::string nickname;
    std::string secret;

    // void display_first_name(std::string f_name);
    // void display_last_name(std::string l_name);
    // void display_nickname(std::string nickname);
    // void display_secret(std::string secret);
    // void display_number(int number);
    // void display_index(int index);
};

#endif